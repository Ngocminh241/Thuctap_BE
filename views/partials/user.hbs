<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel='stylesheet' href='/stylesheets/movieStyle.css' />
    <title>{{title}}</title>
</head>

<body>
    <main>
        <h1 class="title text-center", style="padding-top: 2%;">DANH SÁCH NGƯỜI DÙNG</h1>
        <div class="nav">
            <div class="bgr d-flex flex-row justify-content-between">
                <div class="input-search d-flex flex-row justify-content-between">
                    <input type="search" placeholder="Tìm kiếm..." name="search" id="search-input">
                    <i class="bi bi-search"></i>
                </div>
                <button onclick="BtnAdd()" type="button"><i class="bi bi-plus-circle"></i> Thêm người dùng mới</button>
            </div>
        </div>
        <table border="0" class="table table-striped-columns table-striped">
            <thead>
                <tr>
                    <th>Avatar</th>
                    <th>Tên</th>
                    <th>Vai trò</th>
                    <th class="text-center", style="padding-right: 5%;">Email</th>
                    <th>Số điện thoại</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <div id="preloader"></div>
            <tbody>
                
                <tr>
                    <td>{{this._id}}</td>
                    <td><img src="{{this.profile_picture}}" alt="Avatar" class="avatar-img"></td>
                    <td>{{this.username}}</td>
                    <td>{{this.role}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.phoneNumber}}</td>
                    <td>
                        <button onclick="editUser('{{this._id}}')" type="button" class="btn btn-warning">Sửa</button>
                        <button onclick="deleteUser('{{this._id}}')" type="button" class="btn btn-danger">Xóa</button>
                    </td>
                </tr>
                
            </tbody>
        </table>
        <div class="page">
            <i class="bi bi-caret-left-square" id="ic-giam" onclick="changePage(-1)"></i>
            <input type="text" name="page-number" id="page-number" value="{{currentPage}}" readonly>
            <i class="bi bi-caret-right-square" id="ic-tang" onclick="changePage(1)"></i>
        </div>
        <div class="bgr-dialog" id="bgr-dialog">
            <div class="close-dialog" onclick="closeDialog()"></div>
            <div class="dialog-content" id="dialog-content"></div>
        </div>
    </main>
    <script src="/javascripts/user.js"></script>
</body>

</html>
